<?php

namespace App\Traits;

/**
 * PhotoStorage trait
 * 
 * Currently used as a marker trait for classes that handle photo storage.
 * Future storage-related methods can be added here for shared functionality.
 */
trait PhotoStorage
{
    // Trait is kept for future extensibility and as a marker for photo storage classes
}

